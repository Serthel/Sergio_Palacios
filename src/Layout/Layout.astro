<!DOCTYPE html>
<html lang="es" class="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Portafolio de Thomas A. Anderson (Neo) - Programador, hacker y el Elegido" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" href="https://ext.same-assets.com/408473907/1057939295.ico" />
    <title>{title}</title>
  </head>
  <body class="bg-gray-900 text-gray-100">
    <slot />
    <style>


      html {
        scroll-behavior: smooth;
      }

      body {
        font-family: system-ui, sans-serif;
        margin: 0;
        padding: 0;
        background: rgb(11, 11, 13);
        background: linear-gradient(180deg, rgb(11, 11, 13) 0%, rgb(15, 15, 17) 100%);
        min-height: 100vh;
      }

      /* Custom Matrix green colors */
      :root {
        --matrix-green: #5fa26f;
        --matrix-green-light: #6fb380;
        --matrix-green-dark: #4a8a5c;
        --matrix-bg: #0a0a0a;
        --matrix-bg-card: #111111;
        --matrix-text: #e0e0e0;
        --matrix-text-muted: #c0d0c0;
      }
    </style>
  </body>
</html>